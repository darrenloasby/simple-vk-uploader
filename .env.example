# VK API Token (required)
VK_TOKEN=your_vk_token_here

# Paths (customize as needed)
# Default: $HOME/Videos if not specified
VIDEOS_DIR=$HOME/Videos
WIREGUARD_DIR=$HOME/wireguard
LOGS_DIR=./logs
TRASH_DIR=./trash

# Docker settings (OrbStack recommended for macOS)
DOCKER_BIN=docker
IMAGE_NAME=vk-uploader

# Performance settings
UPLOAD_CHUNK_SIZE_MB=5
PARALLEL_WORKERS=3
MEMORY_LIMIT=2g
MEMORY_SWAP=3g

# Logging
LOG_LEVEL=INFO
MAX_LOG_LINES=1000

# VPN settings
SKIP_WIREGUARD=false  # Set to true to disable VPN

# WireGuard Configurations (portable, non-committed)
# Option 1: Place .conf files in wireguard/ directory (not committed to git)
# Option 2: Base64 encode configs as environment variables (portable)
#
# To convert between formats, use the converter script:
#   ./scripts/wg-config-converter.sh encode    # .conf → env variables
#   ./scripts/wg-config-converter.sh decode    # env variables → .conf
#
# After encoding, the script creates .env.wg-configs that you can append here
# Example output (uncomment and replace with your base64-encoded configs):
# WG_CONFIG_1=W0ludGVyZmFjZV0KUHJpdmF0ZUtleT1...
# WG_CONFIG_1_NAME=privado.syd-012.conf
# WG_CONFIG_2=W0ludGVyZmFjZV0KUHJpdmF0ZUtleT1...
# WG_CONFIG_2_NAME=privado.sin-005.conf
# WG_CONFIG_3=...
# WG_CONFIG_4=...
# WG_CONFIG_5=...

# Timezone (optional)
TZ=Australia/Sydney
